version: "3.9"

services:
  app:
    build: .
    container_name: myapp
    restart: always

    environment:
      - ENV=PROD
      - PRIVATE_KEY_PATH=/app/privatekey.pem
      - CERT_PATH=/app/server.crt

    volumes:
      - /opt/myapp/secrets/privatekey.pem:/app/privatekey.pem:ro
      - /opt/myapp/secrets/server.crt:/app/server.crt:ro

    ports:
      - "80:3000"
      - "443:8443"